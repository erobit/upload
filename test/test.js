// note: test harness will not run until resource is actually a module and included in devDependencies
/*
var resource = require('resource'),
	upload = resource.use('upload');

resource.use('http');

// Testing init method on resource
resource.http.listen({}, function(){
	upload.init({
			app: resource.http.app
		}, function(err, test){

			console.log(err);
	});
});
*/
